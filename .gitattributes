# Ensure shell scripts always use LF line endings (needed for Docker on Windows)
*.sh text eol=lf
docker/entrypoint.sh text eol=lf
scripts/agent-cli-command-names.txt text eol=lf

# Default behavior
* text=auto
